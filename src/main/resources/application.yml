spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_db
    username: root
    password: BreBa-0813
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action:
#                create-target: guru_database_create.sql
