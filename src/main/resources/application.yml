spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_db
    username: root
    password: BreBa-0813
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    defer-datasource-initialization: true
#if you need to use mongo db as database for your application this is the setting
#  data:
#    mongodb:
#      port: 27017
#      host: localhost
#      username: user
#      password: password

#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action:
#                create-target: guru_database_create.sql
